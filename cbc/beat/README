
Weak form:

Parabolic equation:

  <v_t, w> + <m_i grad(v), grad(w)> + <m_i grad(u), grad(w)> + I(v, s)*w = 0

Elliptic equation:

          <m_i grad(v), grad(q)> + <m_{i+e} grad(u), grad(q)> + <I_e, q> = 0

State variables equations:

                   <s_t, r> - F(v, s)*r = 0


for all w \in H^1(\Omega), q, \in H^1(\Omega), r \in L^2(\Omega)

